---

- hosts: localhost
  connection: local
  sudo: yes

  tasks:
    - name: Wait for Docker to be up
      wait_for: path=/var/run/docker.sock state=started delay=3 timeout=5

    - name: Test for Docker status
      command: docker ps
      register: status
      failed_when: "status.rc != 0"
